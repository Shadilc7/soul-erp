<nav class="navbar navbar-expand-lg navbar-custom">
  <div class="container-fluid">
    <%= link_to trainer_portal_root_path, class: "navbar-brand d-flex align-items-center" do %>
      <i class="bi bi-mortarboard-fill me-2"></i>
      <span>BeYa</span>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav me-auto">
        <%= link_to trainer_portal_root_path, class: "nav-link #{'active' if current_page?(trainer_portal_root_path)}" do %>
          <i class="bi bi-speedometer2 me-1"></i> Dashboard
        <% end %>
        <%= link_to trainer_portal_training_programs_path, class: "nav-link #{'active' if controller_name == 'training_programs'}" do %>
          <i class="bi bi-collection me-1"></i> Training Programs
        <% end %>
        <%= link_to list_trainer_portal_attendances_path, class: "nav-link #{'active' if controller_name == 'attendances'}" do %>
          <i class="bi bi-calendar-check me-1"></i> Attendance
        <% end %>
        <%= link_to trainer_portal_training_program_feedbacks_path, class: "nav-link #{'active' if controller_name == 'training_program_feedbacks'}" do %>
          <i class="bi bi-chat-square-text me-1"></i> Feedbacks
        <% end %>
      </ul>

      <div class="user-dropdown">
        <div class="dropdown">
          <button class="btn btn-link dropdown-toggle text-decoration-none text-white" type="button" data-bs-toggle="dropdown">
            <i class="bi bi-person-circle me-1"></i>
            <%= current_user.full_name %>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <%= link_to trainer_portal_profile_path, class: "dropdown-item" do %>
              <i class="bi bi-person me-2"></i> View Profile
            <% end %>
            <%= button_to destroy_user_session_path, method: :delete, class: "dropdown-item text-danger" do %>
              <i class="bi bi-box-arrow-right me-2"></i> Sign Out
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>
